specVersion: "v1"
serviceId: "familia"
location: "europe-central2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "familia-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
